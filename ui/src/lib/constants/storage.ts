export const SESSION_KEY_REFRESH = 'wallet_rt'
export const STORAGE_KEY_USER = 'wallet_user'
export const SESSION_KEY_TRANSACTION_PREFIX = 'wallet_transactions:'
export const SESSION_KEY_PIX_CHARGE_PREFIX = 'wallet_pix_charge:'

/**
 * Where the user was when an outage sent them to the maintenance screen, so the
 * retry returns them there instead of the dashboard.
 */
export const SESSION_KEY_RETURN_AFTER_OUTAGE = 'wallet:return-after-outage'
